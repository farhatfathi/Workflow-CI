name: msml-model

docker_env:
  image: farhatfathi/msml-model:latest

entry_points:
  main:
    parameters:
      n_estimators: {type: int, default: 100}
    command: "python main.py --n_estimators {n_estimators}"
